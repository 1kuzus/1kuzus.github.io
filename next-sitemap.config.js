/** @type {import('next-sitemap').IConfig} */
const nextSitemapConfig = {
    siteUrl: 'https://1kuzus.github.io',
    changefreq: null,
    priority: null,
    generateIndexSitemap: false,
    outDir: 'out',
};

module.exports = nextSitemapConfig;
